{
  "branches": ["main", "master"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "CHART_NAME=$(grep '^name:' Chart.yaml | awk '{print $2}') && sed -i 's/^version:.*/version: ${nextRelease.version}/' Chart.yaml && helm dependency update && helm package . && helm push \"$CHART_NAME-${nextRelease.version}.tgz\" oci://ghcr.io/$GITHUB_REPOSITORY_OWNER"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["Chart.yaml", "CHANGELOG.md"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "common.itsumi-*.tgz",
            "label": "Helm Chart Package"
          }
        ]
      }
    ]
  ]
}
